{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/build/tests/tests", // 실행 파일 경로
            "args": [], // 필요시 실행 인자 추가
            "cwd": "${workspaceFolder}",
            "terminal": "integrated", // VSCode 내 통합 터미널 사용
            "env": {} // 환경 변수 설정
        }
    ]
}
